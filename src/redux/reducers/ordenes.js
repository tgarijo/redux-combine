
const defaultState = [
    {
        total: 100,
    },
    {
        total: 200,
    }
]


function reducer(state = defaultState, action) {
    switch (action.type) {
        
        default:
            return state;
    }
}

export default reducer;
